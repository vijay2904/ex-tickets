CREATE TABLE `images` (name char(20),email varchar(50),movie varchar(100),n0_of_tickets int,genre char(10),phone varchar(10),date date,
 `file_name` varchar(255) COLLATE utf8_unicode_ci NOT NULL,
 `uploaded_on` datetime NOT NULL,
 PRIMARY KEY (`name`)
)